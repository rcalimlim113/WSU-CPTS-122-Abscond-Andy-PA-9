#pragma once

#include "Enemy.h"

class Fireball : public Enemy
{
private:


public:
	////constructor
	//Fireball(int xCoord = 0, int yCoord = 0, int xWidth = 0, int yWidth = 0, int speed = 0)
	//{
	//	setXCoord(xCoord), setYCoord(yCoord), setXWidth(xWidth), setYWidth(yWidth), setName("Fireball"), setSpeed(speed);
	//}

};

